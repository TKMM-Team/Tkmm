<UserControl x:Class="Tkmm.Views.Pages.NetworkSettingsPageView"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:pages="clr-namespace:Tkmm.ViewModels.Pages"
             xmlns:converters="clr-namespace:Tkmm.Converters"
             x:DataType="pages:NetworkSettingsPageViewModel">

    <UserControl.Resources>
        <converters:NullToBoolConverter x:Key="NullToBoolConverter"/>
        <converters:NullableConnectedToBoolConverter x:Key="NullableConnectedToBoolConverter"/>
        <converters:SavedPasswordToBoolConverter x:Key="SavedPasswordToBoolConverter"/>
        <converters:NoSavedPasswordToBoolConverter x:Key="NoSavedPasswordToBoolConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="30" Spacing="20">
            <TextBlock Text="WiFi Networks" FontWeight="Bold" FontSize="32" Margin="0,0,0,20"/>
            <Button Content="Refresh Network List" Command="{Binding ScanForNetworksCommand}" IsEnabled="{Binding IsWifiEnabled}" FontSize="16" Height="40"/>
            <ListBox ItemsSource="{Binding AvailableNetworks}" SelectedItem="{Binding SelectedNetwork}" FontSize="16">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="20">
                            <TextBlock Text="{Binding Ssid}" FontSize="16"/>
                            <TextBlock Text="Connected" Foreground="Green" Margin="10,0,0,0" FontSize="16"
                                       IsVisible="{Binding Connected}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            
            <TextBox Text="{Binding NetworkPassword, Mode=TwoWay}" Watermark="Enter Password" PasswordChar="*"
                     IsVisible="{Binding SelectedNetwork, Converter={StaticResource NoSavedPasswordToBoolConverter}}" FontSize="16" Height="40"/>
            
            <StackPanel Orientation="Horizontal" Spacing="20">
                <Button Content="Connect" Command="{Binding ConnectToNetworkCommand}" IsEnabled="{Binding SelectedNetwork, Converter={StaticResource NullToBoolConverter}}" FontSize="16" Height="40"/>
                <Button Content="Disconnect" Command="{Binding DisconnectSsidCommand}" IsEnabled="{Binding SelectedNetwork, Converter={StaticResource NullableConnectedToBoolConverter}}" FontSize="16" Height="40"/>
                <Button Content="Forget SSID" Command="{Binding ForgetSsidCommand}" IsEnabled="{Binding SelectedNetwork, Converter={StaticResource SavedPasswordToBoolConverter}}" FontSize="16" Height="40"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="30" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="40">
            <TextBlock Text="Network Services" FontWeight="Bold" FontSize="32" Margin="0,0,0,20" TextAlignment="Center"/>
            
            <StackPanel Orientation="Horizontal" Spacing="20" HorizontalAlignment="Center">
                <ToggleSwitch IsChecked="{Binding IsWifiEnabled, Mode=TwoWay}" 
                              OffContent="WiFi Service" OnContent="WiFi Service" FontSize="16" Height="40"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Spacing="20" HorizontalAlignment="Center">
                <ToggleSwitch IsChecked="{Binding IsSshEnabled, Mode=TwoWay}" 
                              OffContent="SSH Service" OnContent="SSH Service" FontSize="16" Height="40"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Spacing="20" HorizontalAlignment="Center">
                <ToggleSwitch IsChecked="{Binding IsSmbEnabled, Mode=TwoWay}" 
                              OffContent="SMB Service" OnContent="SMB Service" FontSize="16" Height="40"/>
            </StackPanel>

            <TextBlock Text="Network Information" FontWeight="Bold" FontSize="32" Margin="0,20,0,20" TextAlignment="Center"/>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Row="0" Grid.Column="0" BorderBrush="{DynamicResource SystemAccentColor}" BorderThickness="1">
                    <TextBlock Text="IP Address:" Margin="30,10,0,10" FontSize="16"/>
                </Border>
                <Border Grid.Row="0" Grid.Column="1" BorderBrush="{DynamicResource SystemAccentColor}" BorderThickness="1">
                    <TextBlock Text="{Binding IpAddress}" Margin="30,10,0,10" FontSize="16"/>
                </Border>

                <Border Grid.Row="1" Grid.Column="0" BorderBrush="{DynamicResource SystemAccentColor}" BorderThickness="1">
                    <TextBlock Text="Subnet Mask:" Margin="30,10,0,10" FontSize="16"/>
                </Border>
                <Border Grid.Row="1" Grid.Column="1" BorderBrush="{DynamicResource SystemAccentColor}" BorderThickness="1">
                    <TextBlock Text="{Binding Netmask}" Margin="30,10,0,10" FontSize="16"/>
                </Border>

                <Border Grid.Row="2" Grid.Column="0" BorderBrush="{DynamicResource SystemAccentColor}" BorderThickness="1">
                    <TextBlock Text="Gateway:" Margin="30,10,0,10" FontSize="16"/>
                </Border>
                <Border Grid.Row="2" Grid.Column="1" BorderBrush="{DynamicResource SystemAccentColor}" BorderThickness="1">
                    <TextBlock Text="{Binding Gateway}" Margin="30,10,0,10" FontSize="16"/>
                </Border>

                <Border Grid.Row="3" Grid.Column="0" BorderBrush="{DynamicResource SystemAccentColor}" BorderThickness="1">
                    <TextBlock Text="MAC Address:" Margin="30,10,0,10" FontSize="16"/>
                </Border>
                <Border Grid.Row="3" Grid.Column="1" BorderBrush="{DynamicResource SystemAccentColor}" BorderThickness="1">
                    <TextBlock Text="{Binding MacAddress}" Margin="30,10,0,10" FontSize="16"/>
                </Border>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>